# ===========================================
# GITHUB ACTIONS WORKFLOW - PR VALIDATION
# Link Chart Frontend - Validação de Pull Requests
# ===========================================

name: 🔍 PR Validation

on:
  pull_request:
    branches: [ main, develop ]
    types: [opened, synchronize, reopened]

# Definir variáveis globais
env:
  NODE_VERSION: '22'

# Definir permissões necessárias
permissions:
  contents: read
  pull-requests: write
  checks: write

jobs:
  # ===========================================
  # JOB 1: VALIDAÇÃO ULTRA-RÁPIDA (SEM VERIFICAÇÕES)
  # ===========================================
  ultra-quick-validation:
    name: ⚡ Ultra-Quick Validation
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 🟢 Setup Node.js ${{ env.NODE_VERSION }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: 📦 Install dependencies
        run: |
          npm ci --prefer-offline --no-audit

      # ===========================================
      # VERIFICAÇÕES AVANÇADAS (COMENTADAS PARA VALIDAÇÃO RÁPIDA)
      # ===========================================
      
      # LINT CHECK (Descomente para ativar)
      # - name: 🔍 ESLint Check
      #   run: npm run lint
      #   continue-on-error: false

      # TYPESCRIPT CHECK (Descomente para ativar)
      # - name: 🔧 TypeScript Check
      #   run: npx tsc --noEmit
      #   continue-on-error: false

      # PRETTIER CHECK (Descomente para ativar)
      # - name: 💅 Format Check
      #   run: npm run format:check
      #   continue-on-error: false

      # TESTES UNITÁRIOS (Descomente para ativar)
      # - name: 🧪 Run Tests
      #   run: npm test
      #   continue-on-error: false

      # AUDIT DE SEGURANÇA (Descomente para ativar)
      # - name: 🔒 Security Audit
      #   run: npm audit --audit-level=moderate
      #   continue-on-error: false

      # ===========================================
      # BUILD ESSENCIAL (MANTIDO)
      # ===========================================
      
      - name: 🏗️ Build Test
        run: |
          npm run build
        env:
          NODE_ENV: production
          NEXT_TELEMETRY_DISABLED: 1

      - name: ✅ PR Validation Success
        run: |
          echo "## 🔍 PR Validation Results" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ **Dependencies**: Installed successfully" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ **Build**: Completed successfully" >> $GITHUB_STEP_SUMMARY
          echo "- 📦 **Bundle Size**: $(du -sh .next/ | cut -f1)" >> $GITHUB_STEP_SUMMARY
          echo "- ⚡ **Status**: Ready for review (Fast Mode)" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 🔧 Verificações Avançadas Desabilitadas:" >> $GITHUB_STEP_SUMMARY
          echo "- ❌ ESLint (comentado para velocidade)" >> $GITHUB_STEP_SUMMARY
          echo "- ❌ TypeScript (comentado para velocidade)" >> $GITHUB_STEP_SUMMARY
          echo "- ❌ Prettier (comentado para velocidade)" >> $GITHUB_STEP_SUMMARY
          echo "- ❌ Tests (comentado para velocidade)" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "💡 **Para ativar verificações completas, edite o workflow**" >> $GITHUB_STEP_SUMMARY

  # ===========================================
  # JOB 2: DOCKER BUILD TEST
  # ===========================================
  docker-test:
    name: 🐳 Docker Build Test
    runs-on: ubuntu-latest
    needs: ultra-quick-validation
    
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 🐳 Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: 🏗️ Test Docker Build
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile
          platforms: linux/amd64
          push: false
          cache-from: type=gha
          cache-to: type=gha,mode=max
          build-args: |
            NODE_ENV=production
            NEXT_TELEMETRY_DISABLED=1

      - name: ✅ Docker Build Success
        run: |
          echo "## 🐳 Docker Build Results" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ **Docker Build**: Completed successfully" >> $GITHUB_STEP_SUMMARY
          echo "- 🏗️ **Platform**: linux/amd64" >> $GITHUB_STEP_SUMMARY
          echo "- 📦 **Ready for**: Production deployment" >> $GITHUB_STEP_SUMMARY
