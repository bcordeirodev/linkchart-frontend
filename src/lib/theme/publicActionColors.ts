/**
 * Coordinated action-button colors shared by the guest success card
 * (`/shorter`) and the public analytics hero (`/public-analytics`), so both
 * rows read as the same family. Every hue is a solid fill dark/saturated enough
 * to carry white text and icons.
 */

/** WhatsApp brand green. */
export const WHATSAPP_GREEN = "#25D366";
/** Slightly darker WhatsApp green for the hover state. */
export const WHATSAPP_GREEN_HOVER = "#1EB257";

/** Near-black charcoal — "view analytics". */
export const ANALYTICS_HUE = "#343843";
/** Deep navy blue — "shorten another". */
export const RESTART_HUE = "#1e3468";
/** Cyan — "share". */
export const SHARE_HUE = "#127d92";
