/**
 * i18n prefixes (under the `tools` namespace) shared by the public
 * `/ferramentas/*` tool pages — `utm` (UTM generator) and `checker`
 * (suspicious-link checker).
 */
export type ToolsI18nKey = "utm" | "checker";
