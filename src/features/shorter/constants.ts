/** Inner content max-width for /shorter sections (form, stats, promos). */
export const SHORTER_CONTENT_MAX_WIDTH = 960;

/** MUI Container `maxWidth` for the /shorter page shell. */
export const SHORTER_PAGE_CONTAINER_MAX_WIDTH = "lg" as const;
